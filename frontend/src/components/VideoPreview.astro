---
import { cloudinaryImage, safePosterUrl } from "../lib/media"

const { mp4, webm, poster } = Astro.props
const safePoster = safePosterUrl(poster, "")
const optimizedPoster = cloudinaryImage(safePoster, 1280)
---

<div class="pp">
  <video
    class="pp-video"
    data-preview-autoplay-video
    muted
    loop
    playsinline
    webkit-playsinline
    preload="none"
    poster={optimizedPoster}
    width="1280"
    height="720"
    disableRemotePlayback
    aria-hidden="true"
  >
    {webm ? <source data-src={webm} type="video/webm" /> : null}
    {mp4 ? <source data-src={mp4} type="video/mp4" /> : null}
  </video>

  <style>
    .pp {
      overflow: hidden;
      border-radius: 0;
    }
    .pp-video {
      display: block;
      width: 100%;
      height: auto;
      aspect-ratio: 16 / 9;
      object-fit: cover;
    }
  </style>
</div>
